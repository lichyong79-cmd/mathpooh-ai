"use client";
import { FormEvent, useState } from "react";

export default function PasswordPage() {
  const [password, setPassword] = useState(""); const [confirm, setConfirm] = useState(""); const [busy, setBusy] = useState(false); const [message, setMessage] = useState("");
  const submit = async (event: FormEvent) => { event.preventDefault(); if (password.length < 6) return setMessage("새 비밀번호는 6자리 이상 입력해 주세요."); if (password !== confirm) return setMessage("비밀번호 확인이 일치하지 않습니다."); setBusy(true); const response = await fetch("/api/student/portal", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ action: "change-password", password }) }); const data = await response.json(); setBusy(false); if (!response.ok) return setMessage(data.message || "변경하지 못했습니다."); alert("비밀번호를 변경했습니다."); window.location.href = "/"; };
  return <main style={{minHeight:"100vh",display:"grid",placeItems:"center",background:"#f2f5fa",padding:20}}><form onSubmit={submit} style={{width:"100%",maxWidth:420,background:"#fff",border:"1px solid #dfe4ee",borderRadius:18,padding:30,display:"grid",gap:15,boxShadow:"0 18px 45px rgba(23,35,63,.1)"}}><small style={{color:"#4d7d46",fontWeight:900}}>MATHPOOH SOS</small><h1 style={{margin:0,color:"#285c31"}}>비밀번호 변경</h1><p style={{margin:0,color:"#707b90"}}>새 비밀번호는 6자리 이상으로 설정해 주세요.</p><label style={{display:"grid",gap:6,fontWeight:800}}>새 비밀번호<input type="password" value={password} onChange={(e)=>setPassword(e.target.value)} style={{height:46,border:"1px solid #ccd3df",borderRadius:10,padding:"0 12px"}} /></label><label style={{display:"grid",gap:6,fontWeight:800}}>새 비밀번호 확인<input type="password" value={confirm} onChange={(e)=>setConfirm(e.target.value)} style={{height:46,border:"1px solid #ccd3df",borderRadius:10,padding:"0 12px"}} /></label>{message?<div style={{color:"#c64545",background:"#fff1f1",padding:10,borderRadius:9}}>{message}</div>:null}<button disabled={busy} style={{height:48,border:0,borderRadius:10,background:"#2f6937",color:"#fff",fontWeight:900}}>{busy?"변경 중...":"비밀번호 변경"}</button><button type="button" onClick={()=>history.back()} style={{height:42,border:"1px solid #d4dae5",borderRadius:10,background:"#fff",fontWeight:800}}>취소</button></form></main>;
}
