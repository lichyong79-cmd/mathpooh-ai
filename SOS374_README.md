# SOS374 시험지 등록 동선 정리

SOS373까지 포함한 전체 코드입니다. 이번 374 변경에 별도 SQL은 없습니다. 372·373을 적용하지 않았다면 SOS373_README.md의 기존 SQL 적용 순서를 먼저 확인하세요.

## 사용 순서
1. 시험지 입력에서 A/B/C 종류 선택 → 종류별 다음 순번과 시험지 이름 자동 입력.
2. 한글 원본·시험지 PDF 업로드. 해설 PDF는 있으면 추가합니다.
3. 파일 저장 후 같은 화면에서 문항 구성·정답·배점 확인.
4. 정답·배점 검수, 표지 미리보기·검수, 문항 자르기·저장·검수.
5. 같은 화면에서 문항 분석·결과 검수 → 등록완료. A/B/C 목록의 해당 순번에 연결됩니다.
6. 다음 시험지 등록 또는 회차별 시험배정으로 이동.

## 변경 사항
- 시험코드·대상학년·과목·범위 입력란 제거. 코드와 과목은 종류 선택에서 자동 생성하며 기존 시험 데이터는 유지합니다.
- 시험지에 응시 시작 날짜를 입력할 필요가 없습니다. 실제 응시 날짜와 시간은 기존 일정 관리에서 설정합니다.
- 빠른답안 없어도 파일 저장과 문항 자르기가 가능합니다. 해설 정답 추출은 선택 기능이며, 빠른정답 표 또는 명시적인 ‘번호. 정답: 값’ 형태를 읽습니다. 이미지 PDF와 다양한 해설 문장 전체를 AI로 해석하는 기능은 아닙니다. 못 읽은 답은 수동 입력하고 검수합니다.
- 저장 후 목록으로 강제 이동하지 않습니다. 재편집 때 저장된 파일을 다시 업로드하지 않습니다.
- PDF 편집기에서 ‘확인하고 다음 미검수’, ‘중간 저장’, ‘저장하고 등록 계속’을 제공합니다. 미저장 이동 경고를 추가했습니다.
- 영역 저장은 삭제 후 삽입 대신 기존 고유키로 일괄 갱신합니다.
- 문항 자르기의 자동 인식·좌표·드래그·잘라내기 함수는 SOS373과 동일합니다.
- 표지를 짙은 녹색·흰색 중심으로 재구성했습니다. 시험 종류·순번·시간·문항수·이름 작성란을 표시합니다. 원본 업로드 PDF의 표지를 자동 교체하지는 않습니다. ‘표지 미리보기·인쇄’에서 사용합니다.
- 단계별 검수 버튼 유지. 정답 검수 시 정답과 배점을 함께 저장하고, 영역 검수는 저장된 영역을 확인합니다.
- 등록완료 시 해당 종류·순번에 다른 시험지가 있으면 덮어쓰지 않습니다.

## 검증 및 확인
Next.js 빌드와 TypeScript 검사를 통과했습니다. 자르기 핵심 함수가 이전 버전과 동일함을 비교했습니다. 실제 Supabase 업로드·저장·인쇄·학생 배정은 운영 계정에서 검증하지 못했습니다.

배포 후 새 시험지 하나로 종류 선택 → 업로드 → 중간 저장 → 자르기 저장 후 복귀 → 검수 → 등록완료를 확인하세요. 기존 학생 답안·점수·학습 데이터에 대한 삭제나 SQL 변경은 없습니다.
