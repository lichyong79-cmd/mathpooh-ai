# SOS125 매쓰푸 공통 작업 오버레이

- 관리자 실전모의고사 전체 재분석: 중앙 대형 `AI 전체 재분석 중!!` 화면, 문항 진행률 표시
- AI 문제은행 분석 화면: 인식·자르기·분석·저장·등록 작업을 동일한 매쓰푸 오버레이로 통일
- 학생 페이지: 시험 준비, 답안 제출/채점, 학생 페이지 초기 로딩을 동일한 오버레이로 통일
- 공통 컴포넌트: `src/components/math-pooh-loader.tsx`
- 수학 곰이 화면 안에서 좌우로 뛰어다니는 CSS 애니메이션과 진행 막대 포함

참고: 작업 환경에 `node_modules`가 없어 `next build`는 실행하지 못했습니다(`next: not found`).
