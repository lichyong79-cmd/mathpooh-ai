# SOS364 배포 안내

## 바뀐 운영 기준

- 학부모가 고르는 것은 콘텐츠 회차가 아니라 **참가 날짜**입니다.
- 시험 당일에는 학생이 실제로 완료한 공식 시험의 다음 순번을 자동 계산합니다.
- 앞선 날짜에 결석해도 공식 시험순번은 올라가지 않습니다.
- 같은 줌 시간에 학생별로 서로 다른 공식순번·범위 시험지를 열 수 있습니다.
- 시험범위는 `대수`, `대수+미적Ⅰ`, `대수+미적Ⅰ+확통` 3종입니다.
- 이전 공식 시험의 SOS가 통과되지 않으면 다음 시험은 시작되지 않습니다.
- 자율형(`SELF`) DB 구조는 포함했지만 현재 신청·운영 화면에서는 `ZOOM`만 사용합니다.

## 기존 기록 처리

- 최현슬·윤동규·배유담의 2026-08-05/12/19/26 기록은 연습시험(공식 0회차)으로 보존합니다.
- 2026-09-09 `SOS 제1회` 제출 기록은 공식 1회차·전체범위로 보존합니다.
- 시험 답안, 점수, 해설 공개 상태와 현재 진행 중인 SOS 학습 데이터는 삭제하거나 다시 만들지 않습니다.
- 정의준처럼 처음 참가하는 학생은 첫 참가 날짜에 공식 1회차 시험지를 받습니다.

## 배포 순서

1. Supabase SQL Editor에서 `supabase-sos364-personal-exam-sequence.sql`을 한 번 실행합니다.
2. SQL이 성공한 뒤 이 코드 버전을 배포합니다.
3. 관리자 `응시 일정·시험지 연결`에서 각 날짜에 필요한 공식순번·범위 시험지를 연결합니다.
4. `일정별 참가자·시험순번`에서 학생별 표시를 확인합니다.
5. 시험 당일 해당 일정의 `전체 시험 시작`을 누릅니다.

코드부터 먼저 배포하면 새 컬럼이 없어 일정 화면 API가 실패할 수 있으므로 반드시 SQL을 먼저 실행합니다.

## 시험 당일 동작

- 연결 직후: 학생·학부모 화면에 예약일과 `시험지 배정 완료`가 표시됩니다.
- 예정시각 1시간 전: 해당 학생에게만 시험지 파일을 제공합니다.
- 예정시각 5분 전: `시험 보러가기`가 열리고 대기실로 들어갑니다.
- 관리자 `전체 시험 시작`: 해당 날짜의 참가자마다 맞는 시험지를 동시에 시작합니다.
- SOS 미통과 또는 시험지 미연결 학생은 시작 대상에서 제외하고 관리자에게 명단을 반환합니다.
- 제출 완료 시에만 참가 상태가 완료되고 다음 공식순번으로 진행합니다.

## AI 문제 생성 운영량

현재 워커는 동시에 4명까지 처리하고, 10문항은 3문항 단위로 저장하며 약 4번의 cron 실행에 나눠 생성합니다. 10분 주기 기준 정상 상태의 이론 처리량은 시간당 약 6명입니다.

- 권장 안정 운영량: **주 40~60명**
- 6일에 분산되고 실패 재시도가 적을 때: **주 80~100명**까지 가능
- 7명이 여러 날에 나눠 사용하는 현재 규모: 충분한 여유가 있습니다.

Supabase 유료 전환은 DB 대기와 저장 안정성에는 도움이 되지만, 수학 생성·조판·독립 재검수 시간은 OpenAI 응답과 Vercel 300초 제한이 주 병목입니다. 따라서 동시처리 수를 무리하게 높이기보다 현재 4명을 유지하는 편이 안전합니다.

## 검증 결과

- `npx tsc --noEmit` 통과
- 실제 환경변수 형식의 더미 값을 넣은 `next build` 전체 59개 페이지 통과
