# SOS303 · 관리자 단계 완료

- SOS 진행관리의 단계 결과에 `이 단계 완료` 버튼을 추가했습니다.
- 전체 문항 응답이 끝난 `진행중/오답중` 단계에서만 완료할 수 있습니다.
- 최초 정오답·교정 기록은 보존하고 세션 상태만 `COMPLETED`로 마감합니다.
- 이후 단계와 다른 회차는 변경하거나 삭제하지 않습니다.
- 관리자 이메일과 완료 당시 정답수를 활동 로그에 기록합니다.

추가 SQL은 없습니다.
