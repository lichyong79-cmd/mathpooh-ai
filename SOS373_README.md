# SOS373 진행 흐름 안전성 보완

371·372를 포함한 누적 전체 코드입니다.

## 적용 순서

1. 372 SQL을 실행하지 않았다면 `supabase-sos372-cycle-scope-sequences.sql`을 먼저 실행합니다.
2. `supabase-sos373-flow-safety.sql` 전체를 실행합니다.
3. 전체 코드를 배포합니다. 진행 중인 시험이 없는 시간에 적용하세요.

## 변경

- 자동저장 직렬화, 제출 중 자동저장 중단, 제출된 답안 덮어쓰기 차단.
- 저장 버전이 달라진 동시 요청은 오류를 반환하며 조용히 덮어쓰지 않습니다.
- 모든 제출 경로의 일정 완료를 DB 트리거로 같은 트랜잭션에서 처리합니다.
- 기존 제출 기록의 미완료 일정 플래그만 보정합니다. 답안·점수·학습은 삭제하지 않습니다.
- 날짜·범위 변경은 DB 함수로 원자적으로 처리하고, 진행 중/완료 기록 변경을 차단합니다.
- 연번은 A/B/C별 실제 제출 완료 기준이며 예약·결석만으로 증가하지 않습니다.
- 다음 일정의 순번은 앞 시험 제출 후 확정됩니다. 미래 5개 시험을 한꺼번에 선배정하는 방식은 지원하지 않습니다.
- A/B/C를 바꾸어도 이전 공식 시험의 SOS 미완료 시 시작을 막습니다. 관리자 OVERRIDE는 유지합니다.
- 학생별 배정 타이머를 보관하여 원본 시험지 재사용 시 이전 타이머를 그대로 가져오지 않습니다.
- 같은 원본 시험지를 서로 다른 일정에서 동시에 진행하는 것은 안전상 차단합니다.
- 관리자 배정 화면에서 A/B/C 변경 시 해당 범위 다음 연번을 다시 선택합니다.

## 검증과 한계

`node scripts/test-sos373.cjs`, TypeScript 검사, production build로 확인합니다.
운영 Supabase에서 SQL 실행·브라우저 종단 테스트는 별도 확인이 필요합니다.
배포 후 테스트 학생으로 신청 → 입금 → 배정 → 타이머 → 시작 → 저장 → 제출 → 결과 → SOS 잠금을 확인하세요.
입금확인의 등록·학생 일정·결제 상태 저장도 한 트랜잭션으로 처리합니다. 계정 생성은 별도 단계입니다.
다중 기기 동시 편집의 답안 병합은 지원하지 않으며 충돌을 오류로 알립니다.
