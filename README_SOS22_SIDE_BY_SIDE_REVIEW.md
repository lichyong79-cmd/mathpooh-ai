# SOS22 · 원본 PDF 옆 검수

별도 SQL 실행은 필요 없습니다.

AI 분석 완료 후 검수 영역은 다음 구조로 표시됩니다.

- 왼쪽: 시험지/해설지 PDF 미리보기
- 오른쪽 위: 문항 목록
- 오른쪽 아래: 선택 문항 수정 및 저장

Git:

```bash
git add .
git commit -m "feat: add side by side PDF review workspace"
git push
```
